import { VariantProps, cva } from "cva";

export const formStyles = cva(
  "",
  {
    variants: {},
    compoundVariants: [],
    defaultVariants: {},
  },
);

export type FormStyleProps = VariantProps<typeof formStyles>;
